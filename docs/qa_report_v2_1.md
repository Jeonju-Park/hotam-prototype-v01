# 호탐 프로토타입 v2.1 검수 리포트 (QA)

> v2.1.0 · 2026-07-11 · 작성: 정주+Claude · 상태: final
> 대조 기준: `docs/handoff_hotam_prototype_spec_v2_1.md` §6 DoD 8항목 + v2.0 기능 회귀
> 검증 방법: 브라우저 실측(원격 사용자 = anon REST 시뮬레이션) + DB 직접 조회 + 정적 스캔

## 요약 — DoD 8/8 통과 · v2.0 회귀 통과

| # | DoD 항목 | 결과 |
|---|---|---|
| 1 | sync_ia: 변경이 change_log에 author='기획자(정주)', 충돌 0 | ✅ 80필드/58행 반영 · 로그 99건 전부 기획자(정주) · 충돌 0 · 재실행 멱등 |
| 2 | 화면 카드+브레드크럼+Type, 진입→연결 ID 칩 이동 | ✅ 전부 실측 |
| 3 | 이슈 8+소통성 5 시딩, 태그·대상 칩·바로가기·댓글·resolved | ✅ 13건 시딩 · 전 기능 실측 |
| 4 | 역할 3종 일관(신규+과거 매핑) | ✅ 구형 '대표' 기록이 '결정권자(대표)'로 표시 |
| 5 | Later·O9 행 [미구현] 뱃지, 동적 판정 | ✅ MutationObserver 기반 · 상태 변화 추종 |
| 6 | 렌즈 아래 집계: 토글·실시간·클릭 필터·미구현 병기 | ✅ 원격 변경 455ms 반영 |
| 7 | 2브라우저 동시: 댓글·마일스톤·집계 3초 내 | ✅ 테이블 454ms · 집계 455ms · 댓글 뱃지 557ms |
| 8 | v2.0 DoD 회귀 + 태그 v2.1.0 배포 | ✅ 회귀 통과 · 본 리포트 후 병합·배포 |

---

## DoD-1 · IA v0.1.1 동기화

- dry-run: **변경 예정 80필드/58행 · 충돌 0** (스펙 "약 61행"과의 차이는 xlsx 행수↔실변경 행수 집계 차 — 변경 패턴은 §0 기술과 일치: ID+이름 병기·내부 용어 제거·비고 정리·소통성 비고 5건).
- `--apply` 후 검증: change_log **99건 전부 author='기획자(정주)'**(sync 80 + 메모 시딩 13 + 확인·해결·댓글 기록), 샘플 값(HTM-S7-11 `→ S4(식당 상세)`) DB·UI 일치.
- 멱등성: 폴백 파일을 v0.1.1로 교체(§0-5)한 뒤 재실행 → "변경 예정 0 · 충돌 0".

## DoD-2 · 화면 카드·브레드크럼·ID 칩 (§1)

- 테이블 상단 화면 카드: `A0` 화면 칩 + 화면명 + Type 칩(Page) + 마일스톤 뱃지 + 목적. 클릭 아코디언에서 카피·이슈·비고 인라인 편집 + 화면 대상 메모 입력(SCREENS 테이블 update, change_log 기록).
- 브레드크럼: 카드 `진입›로그인`, 행 아코디언 `홈›통합검색›결과 탭 — 식당`. **영역 클릭 → 영역 필터('홈' 37행)**, **화면 클릭 → 프로토타입 이동** 실측.
- 진입→연결 칩: `→ S4(식당 상세)`의 `S4` 칩 클릭 → **프로토타입이 S4로 이동**. HTM 칩은 행 점프(+필터 자동 확장). 더블클릭 시 원문 편집(병기 형식 placeholder).

## DoD-3 · 메모 시스템 (§2)

- 시딩: **이슈 8건(O9~O16)** — `[O9] 교차 만족도… · 결정: 대표 · …` 형식, 대상=관련 화면(예: S3) + **소통성 메모 5건**(HTM-S13-01·S14-04·S7-10·S7-14·S9-05, tag=기능). 시드 스크립트 재실행 → "이미 시딩됨"(멱등).
- 실측: 요약 "미해결 이슈 8 · 미해결 기능 5" · 태그 칩 필터(이슈→8건) · 대상 칩 ID+이름 병기("S3 · 비교") · **바로가기 → 프로토타입 S7 이동 + HTM-S7-10 요소 하이라이트 점멸** · 댓글 등록/카운트/Realtime 뱃지 · 해결 토글(목록 제외↔취소선 표시) · 메모 추가 폼(태그 4종 + 대상 자동완성, 기능·화면 태그는 대상 필수 검증).

## DoD-4 · 역할명 (§3)

- 선택지·저장·토스트 = **기획자(정주) / 결정권자(대표) / 개발**. 기존 localStorage '정주' → '기획자(정주)' 자동 이관.
- 과거 데이터 매핑: 구형 author '대표'로 로그를 삽입하는 실험 → 히스토리에 **'결정권자(대표)'로 표시**(DB 원본 불변).

## DoD-5 · 미구현 표시 (§4)

- 판정 = 프레임 DOM의 `data-func-id` 존재 여부(MutationObserver, 하드코딩 목록 0).
- S15 실측: 검색어 입력 전 결과 탭 기능(HTM-S15-02)이 **[미구현] 뱃지+회색 톤**, 입력 후 DOM 생성과 함께 구현으로 전환. 반대로 '최근 검색어'(S15-05)는 입력 후 미구현으로 전환 — 화면 상태 추종 확인. Later 행(HTM-S15-04) 상시 노출+뱃지, 미구현 행은 호버 하이라이트 없음+툴팁("프로토타입 미구현 — IA에만 존재").

## DoD-6 · 마일스톤 집계 (§5)

- 렌즈 아래 잉크 막대(오렌지 0): 현재 화면|전체 토글 — A0 `M1 5` / 전체 `M1 69·M2 30·M3 16·Later 3·결정대기 1`(=119). 미구현 병기(`M2 3 (미구현 1)` — 현재 화면 스코프에서 표기).
- **클릭 필터**: Later 행 클릭 → 테이블이 Later 필터 1행으로 전환(필터 칩으로 해제).
- **실시간**: 원격 마일스톤 변경(M2→M3) 시 집계가 455ms에 M2 30→29 / M3 16→17.

## DoD-7 · 2브라우저 동시 (원격 = anon REST 클라이언트)

| 이벤트 | 상대 화면 반영 | 기준 3초 |
|---|---|---|
| 마일스톤 M2→M3 (결정권자(대표)) | 테이블 드롭다운 **454ms** · 집계 **455ms** · 토스트 "결정권자(대표): HTM-S7-15 마일스톤 M2→M3" | ✅ |
| 메모 댓글 작성 | 새 알림 뱃지 **557ms** | ✅ |

## DoD-8 · v2.0 회귀

- **실시간 동기화**: DoD-7과 동일 시험으로 재확인(±0.5초).
- **양방향 하이라이트**: 요소 호버(HTM-S7-17)→행 워시+자동 스크롤 ✓ / 행 호버(HTM-S7-13)→요소 아웃라인 ✓.
- **CSV 내보내기**: 기능·화면 2파일 + 토스트 ✓.
- **골든 패스**: A0→온보딩→S7→기록(을지면옥골목·좋았어요)→비교 3쌍→S19 **8.4점**→S5 리스트 반영 ✓.
- 빌드 에러 0 · 콘솔: 검수 세션 신규 에러 **0** (버퍼의 80건은 전부 개발 중 파일 편집 시각(HMR 타임스탬프)의 전환기 기록 — 리로드 후 상호작용 전 구간에서 신규 발생 없음).

---

## 수정 내역 (v2.1 개발~검수 중)

| # | 문제 | 수정 |
|---|---|---|
| V21-F1 | nav 칩 셀의 더블클릭 편집이 이벤트 재디스패치 방식이라 무한 루프 위험 | 전용 `NavEditableCell`(자체 편집 상태)로 재설계 |
| V21-F2 | 미구현 판정을 화면 전환 시 1회 스캔 → 화면 내 상태 변화(검색 결과 등) 미추종 | **MutationObserver + 디바운스**로 교체 — 상태 변화 실시간 추종(S15 실측) |
| 참고 | 콘솔의 훅 순서 경고·TableTab 에러 80건 | 개발 중 HMR 전환기 기록으로 판정(전부 편집 시각 타임스탬프, 리로드 후 재현 0) — 코드 수정 불요 |

**부수 기록:** DoD-4 매핑 실험용 change_log 1건('대표' author, "(매핑 테스트)")과 검수 댓글 2건이 기록 보존 원칙에 따라 DB에 남음. tokens.css에 `--status-danger-bg`/`--status-info-bg` 추가(캐논 design_tokens §3의 status(+bg) 대응, 구체값 [추정] — Figma export 시 갱신).

⚠️ 미충족: 없음 — main 병합 + v2.1.0 태그 + 배포 진행.
